using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CL03.Experimental
{
        ///this needs to go first
        [DefaultExecutionOrder(-100)]

        /// <summary>
        /// Handles the input events by caching them into public variables to be accessed.
		///
		/// This may eventually handle direct triggering on a list of delegates to
		/// incur search for its input but that seems not so independent.
        /// </summary>
      public class InputManager : MonoBehaviour

    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
